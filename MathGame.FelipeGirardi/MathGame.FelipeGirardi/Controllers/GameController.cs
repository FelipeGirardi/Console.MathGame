using static MathGame.FelipeGirardi.Enums;

namespace MathGame.FelipeGirardi.Controllers;
internal class GameController
{
    public void startGame(Operations operation)
    {
        int operand1;
        int operand2;

        for(int i = 0; i < 5; i++)
        {
            Console.Clear();
            //operand1 = GenerateOperand1(operation);
            //operand2 = GenerateOperand2(operation, operand1);
        }
    }

    //public int GenerateOperand1(Operations operation)
    //{
    //    Random random = new Random();

    //    if ()
    //}

    //public int GenerateOperand2(Operations operation, int operand1)
    //{
    //    Random random = new Random();
    //}
}
